name: Test Composite Actions

on: [push]

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run Composite Action A
        uses: ./composite-action-a

      - name: Run Composite Action B
        uses: ./composite-action-b
